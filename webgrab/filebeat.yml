filebeat.prospectors:
- type: log
  paths:
    - /usr/share/sitepack/**/*.xml
  tags: ["sitepack"]
  tail_files: false
  #scan_frequency: 10m
  multiline:
    pattern: '<channel'
    negate: true
    match: after

output.logstash:
    hosts: ["logstash:5045"]
    worker: 1
console:
    pretty: true